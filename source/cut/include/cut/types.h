#pragma once
#include <vector>
#include <map>
#include <string>
#include <fstream>
#include <functional>

namespace cut
{
	typedef ::std::function<void()> Lambda_t;

	typedef std::vector<std::string> StringContainer;
}
